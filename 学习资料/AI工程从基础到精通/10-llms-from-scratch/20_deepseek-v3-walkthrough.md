# DeepSeek-V3 架构遍历

> 第十阶段 · 第 14 课列举了每个开放模型调整的六个架构旋钮。DeepSeek-V3（2024 年 12 月，总共 671B 参数，37B 激活）调整了全部六个并添加了四个：多头潜在注意力、无辅助损失负载均衡、多 Token 预测和 DualPipe 训练。本课从头到尾阅读 DeepSeek-V3 的架构，并从已发布的配置推导每个参数计数。最终你将能解释为什么 671B/37B 的比率是正确的赌注，以及为什么 MLA + MoE 一起在前沿领域胜过单独使用。

**类型：** 学习
**语言：** Python（标准库，参数计算器）
**前置知识：** 第十阶段 · 14（开放模型遍历），第十阶段 · 17（NSA），第十阶段 · 18（MTP），第十阶段 · 19（DualPipe）
**时间：** 约 75 分钟

## 学习目标

- 从头到尾阅读 DeepSeek-V3 配置，用六个 GPT-2 旋钮加四个 DeepSeek 特定新增来解释每个字段。
- 推导总参数数量（671B）、激活参数数量（37B）以及贡献到各部分的组件。
- 在 128k 上下文下计算 MLA 的 KV 缓存占用，并与具有 GQA 的相同激活参数稠密模型比较。
- 陈述四个 DeepSeek 特定创新（MLA、MTP、无辅助损失路由、DualPipe），并说出每个针对架构/训练栈的哪个部分。

## 问题

DeepSeek-V3 是第一个架构与 Llama 家族有显著差异的前沿开放模型。Llama 3 405B 是"调整了六个旋钮的 GPT-2"。DeepSeek-V3 是调整了全部六个旋钮再加四个的 GPT-2。阅读 Llama 3 配置是阅读 DeepSeek 配置的热身，但深层结构——注意力块的形状、路由逻辑、训练时目标——足够不同，以至于你需要单独遍历。

学习它的回报：DeepSeek-V3 的开放权重发布改变了开放模型中"前沿能力"的含义。其架构是许多 2026 年训练运行正在复制的蓝图。理解它是任何涉及前沿 LLM 训练或推理职位的入门要求。

## 核心概念

### 不变的核心，再次

DeepSeek-V3 仍然是自回归的。它仍然堆叠解码器块。每个块仍然有注意力加 MLP 加两个 RMSNorm。它仍然在 MLP 中使用 SwiGLU。它仍然使用 RoPE。预归一化。权重绑定嵌入。与每个 Llama 或 Mistral 相同的基线。

### 转折：MLA 而非 GQA

从第十阶段 · 14 你知道 GQA 通过在 Q 头组间共享 K 和 V 来缩小 KV 缓存。多头潜在注意力（MLA）更进一步：K 和 V 被压缩到一个共享的低秩潜在表示中（`kv_lora_rank`），然后在每个头上动态解压缩。KV 缓存只存储潜在表示——通常每个 token 每层 512 个浮点数，而非 8 x 128 = 1024 个浮点数。

在 128k 上下文下，带有 MLA 的 DeepSeek-V3（每个 token 每层一个共享潜在 `c^{KV}`；K 和 V 都从这个潜在通过可被吸收到后续矩阵乘法中的上投影导出）：

```
kv_cache = num_layers * kv_lora_rank * max_seq_len * bytes_per_element
         = 61 * 512 * 131072 * 2
         = 7.6 GB
```

一个假设的 GQA 基线（Llama 3 70B 形状，8 个 KV 头，头维度 128）将付出：

```
kv_cache = 2 * 61 * 8 * 128 * 131072 * 2
         = 30.5 GB
```

MLA 在 128k 上下文下比 Llama-3-70B 风格的 GQA 缓存小 4x。

权衡：MLA 为每个注意力计算（每个头）添加了解压缩步骤。额外计算相比节省的带宽很小。对长上下文推理来说是净收益。

### 路由：无辅助损失负载均衡

MoE 路由器决定哪些 top-k 专家处理每个 token。朴素路由器将过多工作集中在少数专家上，让其他专家闲置。标准修复：添加一个惩罚负载不平衡的辅助损失项。这有效但略微降低主任务性能。

DeepSeek-V3 引入了一个无辅助损失方案。将每个专家的偏置项添加到路由器 logits 中，在训练期间通过简单规则调整：如果专家 `e` 过载，减少 `bias_e`；如果欠载，增加它。没有额外损失项。训练保持干净。专家负载保持均衡。

对主损失的影响：无可测量。对 MoE 架构的影响：更干净，无需调整辅助损失超参数。

### MTP：更密集训练 + 免费草稿

从第十阶段 · 18 你知道 DeepSeek-V3 添加了 D=1 的 MTP 模块，预测两个位置之后的 token。推理时，训练好的模块被重新用作接受率 80%+ 的推测解码草稿。训练时，每个隐藏状态在 D+1 = 2 个目标上被监督，提供更密集的信号。

参数：在 671B 主模型之上的 14B。开销：2.1%。

### 训练：DualPipe

从第十阶段 · 19 你知道 DualPipe 是一个双向流水线，将前向和后向块与跨节点全对全通信重叠。在 DeepSeek-V3 的 2,048 H800 规模下，它回收了 1F1B 会因流水线气泡损失的大约 245k GPU-小时。

### 配置，逐字段

这里是 DeepSeek-V3 配置（简化）：

```
hidden_size: 7168
intermediate_size: 18432   （稠密 MLP 隐藏大小，在前几层使用）
moe_intermediate_size: 2048 （专家 MLP 隐藏大小）
num_hidden_layers: 61
first_k_dense_layers: 3    （前 3 层使用稠密 MLP）
num_attention_heads: 128
num_key_value_heads: 128   （在 MLA 下形式上等于 num_heads，但
                           真正的压缩在 kv_lora_rank 中）
kv_lora_rank: 512          （MLA 潜在维度）
num_experts: 256            （每个块的 MoE 专家数）
num_experts_per_tok: 8      （top-8 路由）
shared_experts: 1           （每个块一个始终启用的共享专家）
max_position_embeddings: 163840
rope_theta: 10000.0
vocab_size: 129280
mtp_module: 1               （深度 1 处 1 个 MTP 模块）
```

解析：

- `hidden_size=7168`：嵌入维度。
- `num_hidden_layers=61`：总块深度。
- `first_k_dense_layers=3`：前 3 个块使用大小为 18432 的稠密 MLP。其余 58 个使用 MoE。
- `num_attention_heads=128`：128 个查询头。
- `kv_lora_rank=512`：K 和 V 被压缩到此潜在维度，并在每个头上解压缩。
- `num_experts=256, num_experts_per_tok=8`：每个 MoE 块有 256 个专家，路由 top-8。
- `shared_experts=1`：在 256 个路由专家之上，1 个始终启用的专家为每个 token 做贡献。将其视为确保每个 token 获得一些可靠内容的"稠密底线"。
- `moe_intermediate_size=2048`：每个专家的 MLP 隐藏大小。比稠密 MLP 小，因为有 256 个专家。

### 参数核算

完整计算在 `code/main.py` 中。头条数字：

- 嵌入：`vocab * hidden = 129280 * 7168 = ~0.93B`。
- 前 3 个稠密块：带 MLA 的注意力（每个块 ~144M）+ 稠密 MLP（每个块 ~260M）+ norms。总计约 1.2B。
- 58 个 MoE 块：带 MLA 的注意力（~144M）+ 256 个专家每个（每个 30M）+ 1 个共享专家（30M）+ norm。总计每个块 ~7.95B，包括所有专家。58 个 MoE 块共 461B。
- MTP 模块：14B。

总计：核心架构 ~476B + 14B MTP，已发布的 671B 数字显然包含了额外的结构参数（偏置张量、专家特定组件、共享专家缩放等）。我们在计算器中再现的数字在已发布值的 3-5% 以内——差异来自 DeepSeek 报告在其第 2 节附录中记录的精细节核算。

每次前向的激活参数：

- 注意力：144M 每层 * 61 = 8.8B（所有层均触发）。
- MLP 激活：前 3 层稠密（3 * 260M = 780M），58 个 MoE 层每个激活 8 个路由 + 1 个共享 + 路由开销。每层激活 MLP：~260M。总计：3 * 260M + 58 * 260M = ~15.9B。
- 嵌入 + norms：1.2B。
- 总激活：大约 26B 核心 + 14B MTP（训练但推理时不总是运行）≈ 37B。

### 671B / 37B 比率

18x 稀疏比（激活参数是总参数的 5.5%）。DeepSeek-V3 是已发布开放权重的最稀疏的前沿 MoE 模型。Mixtral 8x7B 比率 13/47（28%）要密得多。Llama 4 Maverick 比率 17B/400B（4.25%）可比。DeepSeek 的赌注：在前沿规模上，更多专家加更低激活比率在每激活 FLOP 上产生更好的质量。

### DeepSeek-V3 的位置

| 模型 | 总计 | 激活 | 比率 | 注意力 | 新颖想法 |
|-------|------|-------|-------|-----------|-------------|
| Llama 3 70B | 70B | 70B | 100% | GQA 64/8 | — |
| Llama 4 Maverick | 400B | 17B | 4.25% | GQA | — |
| Mixtral 8x22B | 141B | 39B | 27% | GQA | — |
| DeepSeek V3 | 671B | 37B | 5.5% | MLA 512 | MLA + MTP + 无辅助损失 + DualPipe |
| Qwen 2.5 72B | 72B | 72B | 100% | GQA 64/8 | YaRN 扩展 |

### 后续：R1、V4

DeepSeek-R1（2025）是在 V3 骨干上的推理训练运行。R1 使用相同的架构。改变的是后训练配方（在可验证任务上的大规模 RL），而非预训练架构。

DeepSeek-V4（如果发布）预计保持 MLA + MoE + MTP 并添加 DSA（DeepSeek Sparse Attention），即第十阶段 · 17 中 NSA 的后继。谱系是稳定的：架构级别的创新是累积的；每个版本调整额外的旋钮。

## 使用方式

`code/main.py` 是专门针对 DeepSeek-V3 形状的参数计算器。运行它，将输出与论文数字比较，并在假设变体（256 专家 vs 512，top-8 vs top-16，MLA rank 512 vs 1024）上使用。

要看什么：

- 总参数数量 vs 已发布的 671B。
- 激活参数数量 vs 已发布的 37B。
- 128k 上下文下的 KV 缓存——MLA vs GQA 比较。
- 逐层分解以查看参数预算实际去向。

## 交付产出

本课产生 `outputs/skill-deepseek-v3-reader.md`。给定 DeepSeek 家族模型（V3、R1 或任何未来变体），它产生逐组件架构阅读，命名配置的每个字段，按组件推导参数数量，并识别模型使用了四个 DeepSeek 特定创新中的哪些。

## 练习

1. 运行 `code/main.py`。将计算器的总参数估计与已发布的 671B 比较，识别差异来源。论文第 2 节有完整逐项。

2. 修改配置使用 MLA rank 256 而非 512。计算在 128k 上下文下产生的 KV 缓存大小。它带来多少百分比的减少，对每个头表达能力的代价是什么？

3. 将 DeepSeek-V3 的 (256 专家, top-8) 路由与假设的 (512 专家, top-8) 变体比较。总参数增长；激活参数保持相同。额专家容量在理论上带来什么，在推理时付出什么代价？

4. 阅读 DeepSeek-V3 技术报告（arXiv:2412.19437）第 2.1 节关于 MLA。用三句话解释为什么 K 和 V 解压缩矩阵可以被"吸收"到后续矩阵乘法中以实现推理时效率。

5. DeepSeek-V3 对大多数操作使用 FP8 训练。计算 FP8 与 BF16 在存储 671B 权重上的内存节省。这与 14.8T token 训练预算如何关联？

## 关键术语

| 术语 | 人们怎么说 | 实际含义 |
|------|----------------|------------------------|
| MLA | "多头潜在注意力" | 将 K 和 V 压缩到共享低秩潜在表示（kv_lora_rank，通常 512），在每个头上动态解压缩；KV 缓存仅存储潜在表示 |
| kv_lora_rank | "MLA 压缩维度" | K 和 V 的共享潜在表示的大小；DeepSeek-V3 使用 512 |
| 前 k 个稠密层 | "早期层保持稠密" | MoE 模型的前几层跳过 MoE 路由器并运行稠密 MLP 以保持稳定性 |
| num_experts_per_tok | "top-k 路由" | 每个 token 触发多少个路由专家；DeepSeek-V3 使用 8 |
| 共享专家 | "始终启用的专家" | 无论路由如何都处理每个 token 的专家；DeepSeek-V3 使用 1 |
| 无辅助损失路由 | "偏置调整负载均衡" | 在训练期间调整每个专家的偏置项以保持专家负载平衡，而不添加损失项 |
| MTP 模块 | "额外预测头" | 从 h^(1) 和 E(t+1) 预测 t+2 的 transformer 块；更密集训练，免费推测解码草稿 |
| DualPipe | "双向流水线" | 将前向/后向计算与跨节点全对全重叠的训练调度 |
| 激活参数比率 | "稀疏性" | active_params / total_params；DeepSeek-V3 达到 5.5% |
| FP8 训练 | "8 位训练" | 训练存储和许多计算操作使用 FP8；相比 BF16 大约将内存减半，质量代价小 |

## 扩展阅读

- [DeepSeek-AI — DeepSeek-V3 技术报告 (arXiv:2412.19437)](https://arxiv.org/abs/2412.19437) — 完整架构、训练和结果文档
- [DeepSeek-V3 Hugging Face 模型卡](https://huggingface.co/deepseek-ai/DeepSeek-V3) — 配置文件和部署说明
- [DeepSeek-V2 论文 (arXiv:2405.04434)](https://arxiv.org/abs/2405.04434) — 引入 MLA 的前身
- [DeepSeek-R1 论文 (arXiv:2501.12948)](https://arxiv.org/abs/2501.12948) — V3 架构上的推理训练后继
- [原生稀疏注意力 (arXiv:2502.11089)](https://arxiv.org/abs/2502.11089) — DeepSeek 家族注意力的未来方向
- [DualPipe 仓库](https://github.com/deepseek-ai/DualPipe) — 训练调度参考